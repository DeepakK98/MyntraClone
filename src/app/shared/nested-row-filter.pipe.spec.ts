import { NestedRowFilterPipe } from './nested-row-filter.pipe';

describe('NestedRowFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new NestedRowFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
